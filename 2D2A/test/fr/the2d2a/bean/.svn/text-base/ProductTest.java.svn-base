package fr.the2d2a.bean;

import junit.framework.TestCase;
import fr.the2d2a.bean.Produit;

public class ProductTest extends TestCase {

    private Produit product;

    protected void setUp() throws Exception {
        product = new Produit();
    }

    public void testSetAndGetDescription() {
        String testDescription = "aDescription";
        assertNull(product.getReference());
        product.setReference(testDescription);
        assertEquals(testDescription, product.getReference());
    }

    public void testSetAndGetPrice() {
        //double testPrice = 100.00;
        assertEquals(0, 0, 0);    
        product.setVisuLang(0);
        assertEquals("huhu", product.getVisuLang(), 0);
    }
  
}
